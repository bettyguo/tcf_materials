(function(){"use strict";const LS_PREFIX="tcf:";const ls={get(k,fallback){try{const v=localStorage.getItem(LS_PREFIX+k);return v==null?fallback:JSON.parse(v);}catch(e){return fallback;}},set(k,v){try{localStorage.setItem(LS_PREFIX+k,JSON.stringify(v));}catch(e){}},del(k){try{localStorage.removeItem(LS_PREFIX+k);}catch(e){}},};const PAGE_INITS=[];function onPage(fn){PAGE_INITS.push(fn);}
function runAll(){PAGE_INITS.forEach((fn)=>{try{fn();}catch(e){console.error("[tcf]",e);}});}
if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",runAll);}else{runAll();}
if(typeof document$!=="undefined"&&document$.subscribe){document$.subscribe(runAll);}else{document.addEventListener("DOMContentSwapped",runAll);}
let progressEl=null;function ensureProgressBar(){if(progressEl&&document.body.contains(progressEl))return;progressEl=document.querySelector(".tcf-progress");if(!progressEl){progressEl=document.createElement("div");progressEl.className="tcf-progress";progressEl.setAttribute("aria-hidden","true");document.body.appendChild(progressEl);}}
function updateProgress(){if(!progressEl)return;const article=document.querySelector(".md-content article")||document.querySelector(".md-content");if(!article){progressEl.style.width="0%";return;}
const rect=article.getBoundingClientRect();const total=Math.max(1,rect.height-window.innerHeight);const seen=Math.min(total,Math.max(0,-rect.top));const pct=Math.round((seen/total)*100);progressEl.style.width=pct+"%";}
let progressTicking=false;function onProgressScroll(){if(!progressTicking){requestAnimationFrame(()=>{updateProgress();progressTicking=false;});progressTicking=true;}}
onPage(()=>{ensureProgressBar();updateProgress();});window.addEventListener("scroll",onProgressScroll,{passive:true});window.addEventListener("resize",onProgressScroll,{passive:true});onPage(()=>{const bars=document.querySelectorAll(".skill-bar");bars.forEach((bar)=>{const fill=bar.querySelector(".skill-fill");if(!fill)return;const target=fill.style.width||"0%";fill.style.width="0%";bar.style.setProperty("--skill-target",target);});if(!("IntersectionObserver"in window)){bars.forEach((b)=>b.classList.add("is-visible"));return;}
const io=new IntersectionObserver((entries)=>{entries.forEach((e)=>{if(e.isIntersecting){e.target.classList.add("is-visible");io.unobserve(e.target);}});},{threshold:0.4});bars.forEach((b)=>io.observe(b));});function parseStatNum(s){if(!s)return null;const cleaned=s.replace(/[\s ]/g,"");const m=cleaned.match(/^(~?)(\d+)/);if(!m)return null;return{prefix:m[1]||"",n:parseInt(m[2],10),tail:cleaned.slice(m[0].length)};}
function animateNum(el,parsed){const dur=900;const start=performance.now();const ease=(t)=>1-Math.pow(1-t,3);function step(now){const t=Math.min(1,(now-start)/dur);const v=Math.floor(parsed.n*ease(t));el.textContent=parsed.prefix+v.toLocaleString("fr-FR")+parsed.tail;if(t<1)requestAnimationFrame(step);else el.textContent=parsed.prefix+parsed.n.toLocaleString("fr-FR")+parsed.tail;}
requestAnimationFrame(step);}
onPage(()=>{const nums=document.querySelectorAll(".stat-row .stat .num");if(!nums.length||!("IntersectionObserver"in window))return;const io=new IntersectionObserver((entries)=>{entries.forEach((e)=>{if(!e.isIntersecting)return;const el=e.target;if(el.dataset.animated){io.unobserve(el);return;}
const parsed=parseStatNum(el.textContent);if(parsed){el.dataset.animated="1";animateNum(el,parsed);}
io.unobserve(el);});},{threshold:0.5});nums.forEach((n)=>io.observe(n));});const SHORTCUTS=[{keys:["?"],label:"Afficher cette aide"},{keys:["s","/"],label:"Focus sur la recherche"},{keys:["g","h"],label:"Aller à l'accueil"},{keys:["g","d"],label:"Aller au diagnostic"},{keys:["g","r"],label:"Aller à la feuille de route"},{keys:["g","t"],label:"Aller aux outils interactifs"},{keys:["t"],label:"Basculer thème clair / sombre"},{keys:["p"],label:"Imprimer la page"},{keys:["c"],label:"Copier le lien permanent"},{keys:["Esc"],label:"Fermer cette aide"},];let kbdOverlay=null;function ensureKbdOverlay(){if(kbdOverlay&&document.body.contains(kbdOverlay))return;kbdOverlay=document.createElement("div");kbdOverlay.className="tcf-kbd-overlay";kbdOverlay.setAttribute("role","dialog");kbdOverlay.setAttribute("aria-label","Raccourcis clavier");kbdOverlay.setAttribute("aria-modal","true");kbdOverlay.innerHTML='<div class="tcf-kbd-panel" role="document">'+
'<button class="tcf-kbd-close" aria-label="Fermer">×</button>'+
'<h3>Raccourcis clavier</h3>'+
'<div class="tcf-kbd-list">'+
SHORTCUTS.map((s)=>'<div class="tcf-kbd-row"><span>'+escapeHtml(s.label)+'</span><span>'+
s.keys.map((k)=>'<kbd>'+escapeHtml(k)+'</kbd>').join(" + ")+
'</span></div>').join("")+
'</div></div>';document.body.appendChild(kbdOverlay);kbdOverlay.addEventListener("click",(e)=>{if(e.target===kbdOverlay||e.target.classList.contains("tcf-kbd-close")){closeKbdOverlay();}});}
function openKbdOverlay(){ensureKbdOverlay();kbdOverlay.classList.add("is-open");}
function closeKbdOverlay(){if(kbdOverlay)kbdOverlay.classList.remove("is-open");}
let kbdBuffer="";let kbdBufferTimer=null;function resetKbdBuffer(){kbdBuffer="";clearTimeout(kbdBufferTimer);}
document.addEventListener("keydown",(e)=>{const tag=(e.target.tagName||"").toLowerCase();if(tag==="input"||tag==="textarea"||tag==="select"||e.target.isContentEditable)return;if(e.ctrlKey||e.metaKey||e.altKey)return;if(e.key==="Escape"){closeKbdOverlay();resetKbdBuffer();return;}
if(e.key==="?"){e.preventDefault();openKbdOverlay();return;}
if(e.key==="/"||e.key==="s"){const search=document.querySelector(".md-search__input");if(search){e.preventDefault();search.focus();}
return;}
if(e.key==="t"){const themeToggle=document.querySelector('[data-md-component="palette"] form input[name="__palette"]:not(:checked)');if(themeToggle)themeToggle.click();else{const labels=document.querySelectorAll('label[for^="__palette"]');if(labels.length)labels[0].click();}
return;}
if(e.key==="p"){e.preventDefault();window.print();return;}
if(e.key==="c"){e.preventDefault();copyLink();return;}
if(e.key==="g"){kbdBuffer="g";clearTimeout(kbdBufferTimer);kbdBufferTimer=setTimeout(resetKbdBuffer,800);return;}
if(kbdBuffer==="g"){kbdBuffer="";clearTimeout(kbdBufferTimer);const base=document.querySelector('link[rel="canonical"]')?null:null;const baseHref=(document.querySelector('meta[name="site_url"]')||{}).content||(window.__siteBase||"");const map={"h":"/","d":"00_diagnostic/00_index/","r":"../../ROADMAP.md","t":"11_tools/",};if(map[e.key]){e.preventDefault();const dest=map[e.key];if(dest.startsWith("/")){const baseEl=document.querySelector("base");if(baseEl)location.href=baseEl.href+dest.slice(1);else location.href=computeSiteRoot()+dest.slice(1);}else{location.href=computeSiteRoot()+dest;}}
return;}});function computeSiteRoot(){const canonical=document.querySelector('link[rel="canonical"]');if(canonical){const u=new URL(canonical.href);return u.origin+u.pathname.replace(/[^\/]*\/?$/,"");}
return location.origin+"/";}
let fabHost=null;function ensureFabs(){if(fabHost&&document.body.contains(fabHost))return;fabHost=document.createElement("div");fabHost.className="tcf-fab-host";const fabHelp=document.createElement("button");fabHelp.className="tcf-fab";fabHelp.title="Raccourcis clavier (?)";fabHelp.setAttribute("aria-label","Afficher les raccourcis clavier");fabHelp.textContent="?";fabHelp.addEventListener("click",openKbdOverlay);fabHost.appendChild(fabHelp);const fabCopy=document.createElement("button");fabCopy.className="tcf-fab";fabCopy.title="Copier le lien permanent (c)";fabCopy.setAttribute("aria-label","Copier le lien permanent");fabCopy.innerHTML="🔗";fabCopy.addEventListener("click",copyLink);fabHost.appendChild(fabCopy);const fabPrint=document.createElement("button");fabPrint.className="tcf-fab";fabPrint.title="Imprimer la page (p)";fabPrint.setAttribute("aria-label","Imprimer la page");fabPrint.innerHTML="⎙";fabPrint.addEventListener("click",()=>window.print());fabHost.appendChild(fabPrint);const fabTop=document.createElement("button");fabTop.className="tcf-fab is-hidden";fabTop.title="Revenir en haut";fabTop.setAttribute("aria-label","Revenir en haut de page");fabTop.innerHTML="↑";fabTop.addEventListener("click",()=>window.scrollTo({top:0,behavior:"smooth"}));fabHost.appendChild(fabTop);document.body.appendChild(fabHost);window.addEventListener("scroll",()=>{if(window.scrollY>600)fabTop.classList.remove("is-hidden");else fabTop.classList.add("is-hidden");},{passive:true});}
onPage(ensureFabs);function copyLink(){const url=location.href;if(navigator.clipboard&&navigator.clipboard.writeText){navigator.clipboard.writeText(url).then(()=>toast("Lien copié dans le presse-papiers","ok"),()=>toast("Impossible de copier — sélection manuelle nécessaire","warn"));}else{try{const ta=document.createElement("textarea");ta.value=url;document.body.appendChild(ta);ta.select();document.execCommand("copy");ta.remove();toast("Lien copié dans le presse-papiers","ok");}catch(e){toast("Copie indisponible","error");}}}
let toastHost=null;function ensureToastHost(){if(toastHost&&document.body.contains(toastHost))return;toastHost=document.createElement("div");toastHost.className="tcf-toast-host";toastHost.setAttribute("role","status");toastHost.setAttribute("aria-live","polite");document.body.appendChild(toastHost);}
function toast(msg,kind,ms){ensureToastHost();const el=document.createElement("div");el.className="tcf-toast"+(kind?" "+kind:"");el.textContent=msg;toastHost.appendChild(el);requestAnimationFrame(()=>el.classList.add("is-visible"));setTimeout(()=>{el.classList.remove("is-visible");setTimeout(()=>el.remove(),300);},ms||2400);}
window.TCF=window.TCF||{};window.TCF.toast=toast;const NCLC_CO_CE=[{n:10,min:549},{n:9,min:523},{n:8,min:503},{n:7,min:458},{n:6,min:398},{n:5,min:369},{n:4,min:331},{n:3,min:263},];const NCLC_EE_EO=[{n:10,min:18},{n:9,min:16},{n:8,min:14},{n:7,min:12},{n:6,min:10},{n:5,min:7},{n:4,min:4},];function nclcFromRaw(table,raw){if(raw==null||isNaN(raw))return null;for(const row of table){if(raw>=row.min)return row.n;}
return 0;}
function bindMin(co,ce,ee,eo){const arr=[co,ce,ee,eo].filter((x)=>x!=null);if(!arr.length)return null;return Math.min(...arr);}
function crsBonus(min){if(min>=7)return 50;if(min>=5)return 25;return 0;}
onPage(()=>{document.querySelectorAll(".tcf-calc[data-widget=nclc]").forEach(mountNclc);});function mountNclc(host){if(host.dataset.mounted)return;host.dataset.mounted="1";const saved=ls.get("calc:nclc",{});host.innerHTML='<h3>Calculateur NCLC interactif</h3>'+
'<p style="margin:0 0 0.6rem;font-size:0.92rem;opacity:0.82">Entrez vos quatre scores bruts pour voir vos NCLC et le bonus Express Entry estimé. Les valeurs sont sauvegardées dans votre navigateur uniquement.</p>'+
'<div class="calc-grid">'+
calcInput("co","Compréhension orale","/ 699",saved.co)+
calcInput("ce","Compréhension écrite","/ 699",saved.ce)+
calcInput("ee","Expression écrite","/ 20",saved.ee)+
calcInput("eo","Expression orale","/ 20",saved.eo)+
'</div>'+
'<div class="calc-output">'+
outCell("nco","NCLC CO")+
outCell("nce","NCLC CE")+
outCell("nee","NCLC EE")+
outCell("neo","NCLC EO")+
outCell("nmin","NCLC binding (min)")+
outCell("bonus","Bonus CRS estimé")+
'</div>'+
'<div class="verdict" id="tcf-calc-verdict"></div>'+
'<div style="display:flex;gap:0.5rem;margin-top:0.8rem;flex-wrap:wrap">'+
'<button class="tcf-btn" data-act="reset">Réinitialiser</button>'+
'<button class="tcf-btn primary" data-act="permalink">Copier un lien partageable</button>'+
'</div>';function update(){const co=num(host.querySelector('input[name=co]').value);const ce=num(host.querySelector('input[name=ce]').value);const ee=num(host.querySelector('input[name=ee]').value);const eo=num(host.querySelector('input[name=eo]').value);const nco=nclcFromRaw(NCLC_CO_CE,co);const nce=nclcFromRaw(NCLC_CO_CE,ce);const nee=nclcFromRaw(NCLC_EE_EO,ee);const neo=nclcFromRaw(NCLC_EE_EO,eo);setOut(host,"nco",nco);setOut(host,"nce",nce);setOut(host,"nee",nee);setOut(host,"neo",neo);const min=bindMin(nco,nce,nee,neo);setOut(host,"nmin",min);setOut(host,"bonus",min!=null?crsBonus(min)+" pts":"—");const verdict=host.querySelector("#tcf-calc-verdict");if(min==null){verdict.className="verdict";verdict.textContent="Entrez au moins un score pour voir le verdict.";}else if(min>=9){verdict.className="verdict ok";verdict.textContent="Excellent — NCLC 9+ verrouille les 50 pts si l'anglais suit. Maintenez le rythme jusqu'au taper J-7.";}else if(min>=7){verdict.className="verdict ok";verdict.textContent="Cible atteinte — NCLC 7 = 25 pts CRS additifs (50 pts si anglais NCLC ≥ 9). Visez NCLC 8 sur la compétence la plus faible.";}else if(min>=5){verdict.className="verdict warn";verdict.textContent="En zone — encore une compétence à pousser. Identifiez le minimum et concentrez vos prochaines semaines dessus.";}else{verdict.className="verdict bad";verdict.textContent="Sous le seuil — la cible NCLC 7 reste atteignable, mais il faut prioriser la compétence binding (la plus basse).";}
ls.set("calc:nclc",{co,ce,ee,eo});}
host.querySelectorAll("input").forEach((i)=>i.addEventListener("input",update));host.querySelector('[data-act=reset]').addEventListener("click",()=>{host.querySelectorAll("input").forEach((i)=>{i.value="";});ls.del("calc:nclc");update();toast("Calculateur réinitialisé","ok");});host.querySelector('[data-act=permalink]').addEventListener("click",()=>{const params=new URLSearchParams();["co","ce","ee","eo"].forEach((k)=>{const v=host.querySelector('input[name='+k+']').value;if(v!=="")params.set(k,v);});const url=location.origin+location.pathname+"#calc?"+params.toString();navigator.clipboard?.writeText(url).then(()=>toast("Lien partageable copié","ok"),()=>toast("Copie indisponible","warn"));});if(location.hash.startsWith("#calc?")){const params=new URLSearchParams(location.hash.slice(6));["co","ce","ee","eo"].forEach((k)=>{if(params.has(k))host.querySelector('input[name='+k+']').value=params.get(k);});}
update();}
function calcInput(name,label,hint,val){const v=(val==null||val==="")?"":String(val);return'<div class="calc-input">'+
'<label for="tcf-calc-'+name+'">'+escapeHtml(label)+' <span class="hint">'+escapeHtml(hint)+'</span></label>'+
'<input id="tcf-calc-'+name+'" name="'+name+'" type="number" min="0" step="1" value="'+escapeHtml(v)+'" inputmode="numeric" />'+
'</div>';}
function outCell(id,lbl){return'<div class="cell"><span class="val" data-out="'+id+'">—</span><span class="lbl">'+escapeHtml(lbl)+'</span></div>';}
function setOut(host,id,val){const el=host.querySelector('[data-out='+id+']');if(el)el.textContent=(val==null||val===""||(typeof val==="number"&&isNaN(val)))?"—":val;}
function num(v){if(v===""||v==null)return null;const n=parseFloat(v);return isNaN(n)?null:n;}
const TIMER_PRESETS=[{label:"Pomodoro 25/5",work:25*60,rest:5*60,cycles:4},{label:"CO 35 min",work:35*60,rest:0,cycles:1},{label:"CE 60 min",work:60*60,rest:0,cycles:1},{label:"EE T1 10 min",work:10*60,rest:0,cycles:1},{label:"EE T2 20 min",work:20*60,rest:0,cycles:1},{label:"EE T3 30 min",work:30*60,rest:0,cycles:1},{label:"EO total 12 min",work:12*60,rest:0,cycles:1},];onPage(()=>{document.querySelectorAll(".tcf-timer").forEach(mountTimer);});function mountTimer(host){if(host.dataset.mounted)return;host.dataset.mounted="1";let preset=TIMER_PRESETS[0];let total=preset.work;let remaining=total;let interval=null;let mode="work";let cycle=1;host.innerHTML='<div class="clock" aria-live="polite">25:00</div>'+
'<div class="ctrls">'+
'<button class="primary" data-act="toggle">Démarrer</button>'+
'<button data-act="reset">Réinitialiser</button>'+
'<button data-act="skip" title="Sauter à la phase suivante">↦</button>'+
'</div>'+
'<div class="presets" aria-label="Sélecteur de durée">'+
TIMER_PRESETS.map((p,i)=>'<button data-preset="'+i+'"'+(i===0?' class="is-active"':'')+'>'+escapeHtml(p.label)+'</button>').join("")+
'</div>';const clock=host.querySelector(".clock");const toggleBtn=host.querySelector('[data-act=toggle]');function render(){const m=Math.floor(remaining/60);const s=remaining%60;clock.textContent=String(m).padStart(2,"0")+":"+String(s).padStart(2,"0");const phaseLabel=mode==="work"?"Travail":"Pause";clock.setAttribute("aria-label",phaseLabel+" — "+m+" minutes "+s+" secondes");document.title=(interval?"⏱ "+clock.textContent+" — ":"")+(document.__origTitle=document.__origTitle||document.title.replace(/^⏱ \S+\s—\s/,""));}
function start(){if(interval)return;host.classList.add("is-running");host.classList.remove("is-done");toggleBtn.textContent="Pause";interval=setInterval(()=>{remaining--;if(remaining<=0){clearInterval(interval);interval=null;host.classList.remove("is-running");host.classList.add("is-done");toggleBtn.textContent="Démarrer";beep();if(mode==="work"&&preset.rest>0&&cycle<=preset.cycles){mode="rest";total=preset.rest;remaining=total;toast("Travail terminé — pause de "+Math.round(preset.rest/60)+" min","ok");}else if(mode==="rest"&&cycle<preset.cycles){cycle++;mode="work";total=preset.work;remaining=total;toast("Pause terminée — cycle "+cycle+"/"+preset.cycles,"ok");}else{toast("Session terminée !","ok",3500);mode="work";total=preset.work;remaining=total;cycle=1;}
render();}else{render();}},1000);}
function pause(){if(!interval)return;clearInterval(interval);interval=null;host.classList.remove("is-running");toggleBtn.textContent="Reprendre";}
function reset(){pause();mode="work";cycle=1;total=preset.work;remaining=total;host.classList.remove("is-done");toggleBtn.textContent="Démarrer";render();}
function skip(){remaining=1;}
function setPreset(i){preset=TIMER_PRESETS[i];host.querySelectorAll(".presets button").forEach((b,idx)=>b.classList.toggle("is-active",idx===i));reset();}
toggleBtn.addEventListener("click",()=>interval?pause():start());host.querySelector('[data-act=reset]').addEventListener("click",reset);host.querySelector('[data-act=skip]').addEventListener("click",skip);host.querySelectorAll(".presets button").forEach((b)=>b.addEventListener("click",()=>setPreset(parseInt(b.dataset.preset,10))));render();}
function beep(){try{const ctx=new(window.AudioContext||window.webkitAudioContext)();const o=ctx.createOscillator();const g=ctx.createGain();o.connect(g);g.connect(ctx.destination);o.type="sine";o.frequency.value=880;g.gain.setValueAtTime(0.001,ctx.currentTime);g.gain.exponentialRampToValueAtTime(0.3,ctx.currentTime+0.05);g.gain.exponentialRampToValueAtTime(0.0001,ctx.currentTime+0.6);o.start();o.stop(ctx.currentTime+0.7);}catch(e){}}
window.TCF.quizzes=window.TCF.quizzes||{};onPage(()=>{document.querySelectorAll(".tcf-quiz[data-quiz]").forEach(mountQuiz);});function mountQuiz(host){if(host.dataset.mounted)return;const key=host.dataset.quiz;const data=window.TCF.quizzes[key];if(!data||!Array.isArray(data)||!data.length){host.innerHTML='<p style="opacity:0.7">Quiz « '+escapeHtml(key)+' » introuvable.</p>';return;}
host.dataset.mounted="1";let idx=0;let score=0;const answers=[];function render(){const item=data[idx];if(!item){renderSummary();return;}
const total=data.length;const letters=["A","B","C","D","E"];host.innerHTML='<div class="progress-row"><span>Question '+(idx+1)+' / '+total+'</span><span>Score : '+score+'</span></div>'+
'<div class="progress-bar"><div style="width:'+Math.round((idx/total)*100)+'%"></div></div>'+
'<div class="question">'+escapeHtml(item.q)+'</div>'+
'<div class="options">'+
item.options.map((o,i)=>'<button type="button" data-i="'+i+'"><span class="marker">'+letters[i]+'</span>'+escapeHtml(o)+'</button>').join("")+
'</div>'+
'<div class="feedback" hidden></div>'+
'<div style="margin-top:0.6rem;display:flex;justify-content:flex-end;gap:0.4rem">'+
'<button class="tcf-btn" data-act="next" hidden>'+(idx===total-1?"Voir le résultat →":"Question suivante →")+'</button>'+
'</div>';const opts=host.querySelectorAll(".options button");const feedback=host.querySelector(".feedback");const next=host.querySelector('[data-act=next]');opts.forEach((btn)=>{btn.addEventListener("click",()=>{const choice=parseInt(btn.dataset.i,10);const correct=choice===item.answer;opts.forEach((b,i)=>{b.disabled=true;if(i===item.answer)b.classList.add("correct");else if(i===choice)b.classList.add("incorrect");});feedback.hidden=false;feedback.className="feedback "+(correct?"ok":"bad");feedback.innerHTML=(correct?"✓ Bonne réponse. ":"✗ Mauvaise réponse — la bonne réponse était "+letters[item.answer]+". ")+
(item.explain?'<br><em>'+escapeHtml(item.explain)+'</em>':"");if(correct)score++;answers.push({i:idx,correct});next.hidden=false;});});next.addEventListener("click",()=>{idx++;render();});}
function renderSummary(){const total=data.length;const pct=Math.round((score/total)*100);const verdict=pct>=80?"ok":(pct>=60?"warn":"bad");const message=pct>=80?"Solide. Vous maîtrisez cette unité.":(pct>=60?"Acceptable. Revoyez les items manqués ci-dessous.":"À retravailler. Reprenez les sections en amont.");host.innerHTML='<div class="question">Score final : '+score+' / '+total+' ('+pct+' %)</div>'+
'<div class="feedback '+(verdict==="bad"?"bad":"ok")+'">'+escapeHtml(message)+'</div>'+
'<div style="margin-top:1rem;font-size:0.92rem">'+
'<strong>Détail :</strong><br>'+
answers.map((a)=>'<div style="padding:0.25rem 0">'+(a.correct?"✅":"❌")+' Q'+(a.i+1)+' — '+escapeHtml(data[a.i].q.slice(0,80))+(data[a.i].q.length>80?"…":"")+'</div>').join("")+
'</div>'+
'<div style="margin-top:1rem;display:flex;gap:0.5rem;flex-wrap:wrap">'+
'<button class="tcf-btn primary" data-act="restart">Recommencer</button>'+
'</div>';host.querySelector('[data-act=restart]').addEventListener("click",()=>{idx=0;score=0;answers.length=0;render();});}
render();}
onPage(()=>{document.querySelectorAll(".tcf-checklist[data-key]").forEach(mountChecklist);});function mountChecklist(host){if(host.dataset.mounted)return;host.dataset.mounted="1";const key="check:"+host.dataset.key;const state=ls.get(key,{});const items=host.querySelectorAll("li");let done=0;items.forEach((li,i)=>{const id=li.dataset.id||("item-"+i);const cb=document.createElement("input");cb.type="checkbox";cb.id=key+":"+id;cb.checked=!!state[id];if(cb.checked){li.classList.add("is-done");done++;}
const text=document.createElement("span");text.innerHTML=li.innerHTML;li.innerHTML="";li.appendChild(cb);li.appendChild(text);li.addEventListener("click",(e)=>{if(e.target.tagName==="A")return;if(e.target!==cb)cb.checked=!cb.checked;state[id]=cb.checked;ls.set(key,state);li.classList.toggle("is-done",cb.checked);updateSummary();});});const summary=document.createElement("div");summary.className="summary";host.appendChild(summary);function updateSummary(){const total=items.length;const checked=Array.from(items).filter((li)=>li.classList.contains("is-done")).length;const pct=total?Math.round((checked/total)*100):0;summary.innerHTML='<span>'+checked+' / '+total+' ('+pct+' %)</span>'+
'<button class="tcf-btn" data-act="reset" style="font-size:0.78rem;padding:0.25rem 0.6rem">Tout décocher</button>';summary.querySelector('[data-act=reset]').addEventListener("click",()=>{if(!confirm("Tout décocher cette liste ?"))return;items.forEach((li)=>{li.classList.remove("is-done");const cb=li.querySelector("input[type=checkbox]");if(cb)cb.checked=false;});ls.del(key);Object.keys(state).forEach((k)=>delete state[k]);updateSummary();toast("Liste réinitialisée","ok");});}
updateSummary();}
onPage(()=>{document.querySelectorAll("audio:not([data-tcf-rate])").forEach((audio)=>{audio.dataset.tcfRate="1";const wrap=document.createElement("div");wrap.style.display="flex";wrap.style.gap="0.4rem";wrap.style.margin="0.3rem 0 0.8rem";wrap.style.alignItems="center";wrap.style.fontSize="0.78rem";wrap.style.opacity="0.85";const label=document.createElement("span");label.textContent="Vitesse :";wrap.appendChild(label);[0.75,0.9,1.0,1.1,1.25].forEach((r)=>{const b=document.createElement("button");b.type="button";b.className="tcf-btn";b.style.padding="0.15rem 0.5rem";b.style.fontSize="0.78rem";b.textContent=r+"×";if(r===1.0)b.classList.add("primary");b.addEventListener("click",()=>{audio.playbackRate=r;wrap.querySelectorAll("button").forEach((x)=>x.classList.remove("primary"));b.classList.add("primary");});wrap.appendChild(b);});audio.insertAdjacentElement("afterend",wrap);});});onPage(()=>{const host=location.host;document.querySelectorAll(".md-content a[href^=http]").forEach((a)=>{if(a.dataset.tcfExt||a.host===host)return;a.dataset.tcfExt="1";a.setAttribute("rel",(a.getAttribute("rel")||"")+" noopener");a.setAttribute("target","_blank");if(a.querySelector("svg")||a.closest(".md-source, .md-content__button, .md-social, .md-footer-social"))return;const arrow=document.createElement("span");arrow.setAttribute("aria-hidden","true");arrow.textContent=" ↗";arrow.style.fontSize="0.75em";arrow.style.opacity="0.65";a.appendChild(arrow);});});onPage(()=>{if(document.querySelector(".tcf-skip"))return;const a=document.createElement("a");a.className="tcf-skip";a.href="#";a.textContent="Aller au contenu";a.addEventListener("click",(e)=>{e.preventDefault();const main=document.querySelector(".md-content")||document.querySelector("main");if(main){main.setAttribute("tabindex","-1");main.focus();main.scrollIntoView({behavior:"smooth",block:"start"});}});document.body.insertBefore(a,document.body.firstChild);});function escapeHtml(s){return String(s==null?"":s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;");}
window.TCF.ls=ls;window.TCF.nclcFromRaw=nclcFromRaw;window.TCF.nclcTables={CO_CE:NCLC_CO_CE,EE_EO:NCLC_EE_EO};})();